# COBOL Insight 導入手順書

本書は、COBOL Insight を Windows 11 の端末へ導入し、動作を確認するまでの手順を定める。
署名なしの配布物であるため、SmartScreen の警告への対処を第5章で扱う。

## 1. 動作環境

- **OS**: Windows 11(x64)
- **ディスク空き容量**: 400 MB 以上(インストーラの一時展開分を含む)
- **メモリ**: 4 GB 以上
- **ネットワーク**: 不要。導入も解析もローカルで完結する。
- **事前に導入が要るソフトウェア**: なし。Java 実行環境と Chromium は配布物へ同梱している。

## 2. 配布物

| ファイル | 内容 |
|---|---|
| `COBOL Insight Setup 0.1.0.exe` | NSIS インストーラ。GUI 本体・Chromium・解析エンジン(内蔵 JRE 同梱)を含む |

配布物にはコード署名を施していない。署名証明書を調達しない方針であり、SmartScreen は
第5章の手順で通す。

## 3. 対話形式での導入

1. `COBOL Insight Setup 0.1.0.exe` をダブルクリックする。
2. SmartScreen の警告が出た場合は、第5章の手順で実行へ進む。
3. インストール先を選ぶ画面で、既定の候補をそのまま使うか、任意のフォルダを指定する。
   管理者権限を要さない利用者単位の導入であり、既定の候補も利用者のプロファイル配下である。
4. 導入の完了後、スタートメニューの「COBOL Insight」から起動する。

## 4. サイレント導入

複数の端末へ配る場合は、次のようにインストーラを起動する。管理者権限を要さない。

```powershell
& "COBOL Insight Setup 0.1.0.exe" /S /D=C:\Apps\COBOLInsight
```

- `/S`: 画面を出さずに導入する。
- `/D=<パス>`: 導入先を明示する。`/D` は最後の引数に置き、パスを引用符で囲まない。

## 5. SmartScreen の警告への対処

署名なしの実行ファイルであるため、Microsoft Defender SmartScreen が
「WindowsによってPCが保護されました」と表示して実行を止める場合がある。次の手順で進む。

1. 警告の画面で「詳細情報」を押す。
2. 現れた「実行」を押す。

ダウンロードで取得したファイルの場合は、実行の前にブロックを解除する方法もある。

1. インストーラのファイルを右クリックし、「プロパティ」を開く。
2. 「全般」タブの下部に「セキュリティ: このファイルは他のコンピューターから取得したものです」
   と出ていれば、「許可する」をチェックして「OK」を押す。

PowerShell からブロックを解除する場合は次のとおりである。

```powershell
Unblock-File -Path "COBOL Insight Setup 0.1.0.exe"
```

SmartScreen の表示は、端末の Defender 設定・配布経路・当該ファイルの評価の蓄積によって変わる。
導入先の端末で実際の表示を目視で確かめ、次の記録欄へ記入する。

| 確認項目 | 記録 |
|---|---|
| 確認日 | |
| 端末の OS ビルド | |
| 取得経路(共有フォルダ / ダウンロード / USB 等) | |
| SmartScreen の表示(出た / 出ない) | |
| 表示された文言 | |
| 実行までに要した操作 | |

## 6. 起動と初回の動作確認

1. 「COBOL Insight」を起動する。初回起動はディスクキャッシュが温まっていないため、
   2回目以降より時間がかかる。
2. 資産エクスプローラーの「＋ インポート」を押し、COBOL 資産のフォルダを選ぶ。
3. 「▶ 解析実行」を押す。資産一覧に文字コードと解析状態が並べば、解析エンジンが正しく
   起動し、永続化が機能している。
4. 「指摘一覧」タブへ移り、検出結果が並ぶことを確かめる。

解析エンジンは同梱の実行ファイルを子プロセスとして起動する。端末側に Java を導入する必要はない。

## 7. ネットワークを使わないことの確認

導入先の運用規程で通信が発生しないことを確かめる必要がある場合は、次の手順で観測する。

```powershell
$app = Get-Process -Name "COBOL Insight"
$ids = @($app.Id) + (Get-CimInstance Win32_Process |
  Where-Object { $_.ParentProcessId -in $app.Id } | ForEach-Object { $_.ProcessId })
Get-NetTCPConnection -OwningProcess $ids -ErrorAction SilentlyContinue
```

解析の実行中を含めて、出力が空であれば TCP 接続を持たない。COBOL Insight は
GUI・解析エンジンのいずれもネットワークへ接続せず、外部の API・CDN・更新確認も呼ばない。

## 8. 削除

「設定」→「アプリ」→「インストールされているアプリ」から「COBOL Insight」を選び、
アンインストールする。導入先フォルダの `Uninstall COBOL Insight.exe` からも削除できる。
解析結果を保存した SQLite ファイル・レポート・逐語対訳の生成物は、利用者が指定した
出力先に残るため、必要に応じて手動で削除する。

## 9. 制約事項

- 配布物へコード署名を施していない。SmartScreen の警告は第5章の手順で通す。
- 実行ファイルのバージョン情報(製品名・説明・発行者)を書き込んでいない。署名処理の
  スキップと同じ設定(`signAndEditExecutable: false`)に伴うものであり、動作へは影響しない。
- 導入・解析・レポート出力のすべてがローカルで完結する。自動更新の機能を持たない。
  新しいバージョンへ移るときは、新しいインストーラを同じ手順で導入する。

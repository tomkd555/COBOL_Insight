# 配布計測記録

本書は、COBOL Insightの生成済み配布物、すなわち解析エンジンのjpackage app-imageと、
GUIのelectron-builder+NSISインストーラを実機Windows 11で実測し、配布サイズ・導入後の
ディスクサイズ・起動時間・同梱物・起動中の通信有無を記録したものである。
`engine/distribution-notes.md`に記録されたM1(骨格main段階)の基礎値との対比を第3章に示す。

なお`docs/02_要件定義.md`は性能の数値目標を定めず(4.1節)、Electronと内蔵JREの二重
ランタイムによる配布サイズと起動時間の増加を内部ツールとして受容する方針を採る。本書は
その方針のもとで、実測値を事実として残すためのものである。

## 測定環境

- OS: Windows 11 Home 10.0.26200.8875
- JDK: Eclipse Temurin 21.0.11+10(`openjdk version "21.0.11" 2026-04-21 LTS`、
  `OpenJDK Runtime Environment Temurin-21.0.11+10`)
- Node.js: v24.14.0
- npm: 11.9.0
- Electron: 33.4.11(`gui/package-lock.json`記載の実導入版数)
- electron-builder: 25.1.8(同上)

## 1. 解析エンジン(jpackage app-image)

測定対象は`engine/cli/build/jpackage/app-image/COBOLInsight`である。`gradle
:engine:cli:jpackageAppImage --offline`で生成した成果物を実測した。

### 1.1 サイズ

- ディスクサイズ合計: 278,061,140バイト(265.18 MB)
- うち`app/`(cli.jarと依存ライブラリ一式): 119,777,576バイト(114.23 MB)、78ファイル
- うち`runtime/`(同梱JRE): 157,832,492バイト(150.52 MB)、350ファイル
- `COBOLInsight.exe`本体: 451,072バイト

### 1.2 起動時間

`--help`実行の起動から終了までを`Measure-Command`で3回計測した。

- 1回目: 3,207.95 ms
- 2回目: 271.60 ms
- 3回目: 273.77 ms

1回目はディスクキャッシュが温まっていないため大きく伸び、2回目・3回目は271〜274 msで
安定する。

### 1.3 ライセンス同梱

`app/cli.jar`の内部リソースとして、次の2件が存在する。

- `licenses/che4z/LICENSE.md`(46,628バイト、Che4z、EPL-2.0)
- `licenses/mapa/LICENSE`(1,097バイト、MAPA、MIT)

両ファイルは`app/`直下に独立したファイルとしては存在せず、`cli.jar`というjarアーカイブ
の内部にのみ格納されている。

### 1.4 通信の観測

`samples/`(bms 1件・cobol 9件・copybook 3件・jcl 3件、計16ファイル)を入力に`scan`を
実行し、プロセス生存中`Get-NetTCPConnection -OwningProcess`を20 ms間隔で呼んだ。

- プロセスID: 35268
- ポーリング回数: 21回
- 検出されたTCP接続数: 0件
- `scan`の結果: 終了コード0、analyzed 16件・skipped 0件・findingCount 0件

## 2. GUI(electron-builder + NSIS)

### 2.1 インストーラのファイルサイズ

`gui/dist/installer/COBOL Insight Setup 0.1.0.exe`を実測した。

- 210,853,953バイト(201.09 MB)

### 2.2 導入後のディスクサイズ

scratchpad配下の一時導入先へ、サイレント導入(`/S /D=<導入先>`)を行った。導入プロセスは
終了コード0で完了した。

- 導入後ディスクサイズ合計: 603,970,729バイト(575.99 MB)、504ファイル

### 2.3 同梱物の確認

- `<導入先>\resources\engine\COBOLInsight.exe`の存在を確認した(451,072バイト)。
- 上記実行ファイルへ`--help`を渡して実行した結果、終了コード0、標準出力の先頭は次の
  とおりである。

```
Usage: cobol-insight [-hV] [COMMAND]
COBOL資産の統合解析ツール
  -h, --help      Show this help message and exit.
  -V, --version   Print version information and exit.
Commands:
  scan        資産フォルダを解析し、結果をSQLiteへ永続化する
  callgraph   呼出関係グラフを構築してSQLiteへ保存し、JSON/DOT/SVG/PNGで出力する
  lint        資産フォルダをルールで静的解析し、SARIFを出力する
  sql-advise  資産フォルダの埋め込みSQLをSQL助言ルールで解析し、SARIFを出力する
  report      scan/lint/sql-advise/callgraph の結果を統合し、HTML/テキストのレポートを生成する
  transpile   資産フォルダの COBOL を Python/Java へ逐語対訳し、行対応表を保存する
  fix         検出結果への修正案を差分表示(preview)・修正後ソース出力(apply)する
```

- `resources\app.asar`の存在を確認した(44,023,282バイト)。
- asar外へ展開された`sql-wasm.wasm`は
  `resources\app.asar.unpacked\node_modules\sql.js\dist\sql-wasm.wasm`
  (652,953バイト)に存在する。`gui/package.json`の`build.asarUnpack`設定により、
  このファイルだけがasar外へ展開される。

### 2.4 起動時間

計測方法はM1と同じである。`Start-Process`で導入後の`COBOL Insight.exe`を起動し、起動
直後から`Process.MainWindowHandle`がゼロでなくなるまでの経過時間を`Stopwatch`で計測
した。3回連続実行し、各回の計測後は`CloseMainWindow`のあと、必要に応じて`Kill`で
プロセスを終了させた。各回の終了後、「COBOL Insight」という名前のプロセスが残っていない
ことを`Get-Process`で確認した。

- 1回目(導入直後の初回起動): 6,205.60 ms
- 2回目: 1,304.20 ms
- 3回目: 1,501.77 ms

### 2.5 通信の観測

導入後の`COBOL Insight.exe`を起動し、GUI本体プロセスと子プロセス群のプロセスIDを
`Get-CimInstance Win32_Process`で収集しながら、`Get-NetTCPConnection -OwningProcess
<ID群>`を約50 ms間隔で呼んだ。

- 収集したプロセスID: 35528(GUI本体)、10672、9868、22184(子プロセス)
- ポーリング回数: 3回(WMI問い合わせ自体の所要時間により、4秒間の観測窓で3回の
  ポーリングにとどまった)
- 検出されたTCP接続数: 0件

### 2.6 通信を行わないことの構造的な根拠

2.5の観測窓は起動と待機の区間に限られる。解析中を含めた全区間を観測で覆うことはできない
ため、実装側の根拠を併記する。

- `gui/src`と`gui/smoke`の全ソースに対する検索で、`fetch(`・`XMLHttpRequest`・
  `WebSocket`・`node:net`/`http.request`の使用は0件である。外部URLの記載は、同梱フォント
  のライセンス文(`src/renderer/src/assets/fonts/OFL.txt`)だけである。
- rendererのContent-Security-Policyは
  `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; worker-src 'self' blob:; object-src 'none'; base-uri 'self'`
  であり、外部オリジンを一切許可しない。実描画smokeが、CSP拒否とconsoleエラーが0件で
  あることを毎回検査する。
- GUIと解析エンジンの間はループバックソケットを用いない。GUIはエンジンを子プロセスとして
  起動し、標準出力の末尾1行のサマリJSONと、SQLiteプロジェクトファイル・SARIF・HTML等の
  出力ファイルから結果を受ける。
- 自動更新・クラッシュレポート送信・利用統計送信の機能を持たない。

## 3. M1時点の基礎値との対比

### 3.1 解析エンジン

| 項目 | M1(骨格main・既定JRE同梱) | M8(engine実装完了時点) | 差 |
|---|---|---|---|
| ディスクサイズ | 211,963,174バイト(202.14 MB) | 278,061,140バイト(265.18 MB) | +66,097,966バイト(+63.04 MB) |
| 起動〜終了(`--help`、1回目) | 100.39 ms | 3,207.95 ms | +3,107.56 ms |
| 起動〜終了(`--help`、2回目) | 92.17 ms | 271.60 ms | +179.43 ms |
| 起動〜終了(`--help`、3回目) | 90.47 ms | 273.77 ms | +183.30 ms |

ディスクサイズの増加は、cobol-frontend・jcl-frontend・sql-frontend・persistence・
linker・rules・dataflow・transpile・fix・graphviz-java・GraalJS等の実装モジュールが
依存ライブラリとして`app/`配下に加わったことによる。

### 3.2 GUI

| 項目 | M1(骨格main段階) | M8(実装完了時点) | 差 |
|---|---|---|---|
| インストーラサイズ | 81,694,729バイト(77.90 MB) | 210,853,953バイト(201.09 MB) | +129,159,224バイト(+123.18 MB) |
| 導入後ディスクサイズ | 281,236,664バイト(268.21 MB) | 603,970,729バイト(575.99 MB) | +322,734,065バイト(+307.78 MB) |
| 起動時間(初回) | 5,216.64 ms | 6,205.60 ms | +988.96 ms |
| 起動時間(2回目) | 140.44 ms | 1,304.20 ms | +1,163.76 ms |
| 起動時間(3回目) | 140.06 ms | 1,501.77 ms | +1,361.71 ms |

GUI側のディスクサイズ増加は、React画面本体・Monaco Editor・Cytoscape等の描画ライブラリ
の追加と、`gui/package.json`の`extraResources`設定による解析エンジンapp-image一式の
同梱が主因である。M1の骨格main・単一HTML画面はこれらを持たない。起動時間の増加は、
画面本体・同梱ライブラリの読み込み量の増加を反映していると考えられる。

## 4. 実行したコマンド全文

### 4.1 インストーラのファイルサイズ計測

```powershell
$installer = "C:\Mycode\ClaudeCode\COBOL_Insight\gui\dist\installer\COBOL Insight Setup 0.1.0.exe"
(Get-Item $installer).Length
```

### 4.2 サイレント導入

```powershell
$installer = "C:\Mycode\ClaudeCode\COBOL_Insight\gui\dist\installer\COBOL Insight Setup 0.1.0.exe"
$installDir = "<scratchpad配下の一時導入先>\install-m8"
$proc = Start-Process -FilePath $installer -ArgumentList "/S", "/D=$installDir" -PassThru
$proc.WaitForExit()
$proc.ExitCode
```

### 4.3 導入後ディスクサイズ計測

```powershell
$files = Get-ChildItem -Recurse -File -Path $installDir
($files | Measure-Object -Property Length -Sum).Sum
```

### 4.4 同梱物の確認

```powershell
Test-Path "$installDir\resources\engine\COBOLInsight.exe"
& "$installDir\resources\engine\COBOLInsight.exe" --help
Get-Item "$installDir\resources\app.asar"
Get-ChildItem -Recurse "$installDir\resources\app.asar.unpacked" -File
```

### 4.5 起動時間計測(3回のうち1回分、残り2回も同一手順)

```powershell
$exe = "$installDir\COBOL Insight.exe"
$sw = [System.Diagnostics.Stopwatch]::StartNew()
$proc = Start-Process -FilePath $exe -PassThru
while ($proc.MainWindowHandle -eq 0) {
    Start-Sleep -Milliseconds 5
    $proc.Refresh()
}
$sw.Stop()
$sw.Elapsed.TotalMilliseconds
$proc.CloseMainWindow() | Out-Null
Start-Sleep -Milliseconds 800
if (-not $proc.HasExited) { $proc.Kill() }
$proc.WaitForExit(5000)
```

### 4.6 無通信観測

```powershell
$proc = Start-Process -FilePath $exe -PassThru
$mainId = $proc.Id
$allPids = New-Object System.Collections.Generic.HashSet[int]
$allPids.Add($mainId) | Out-Null
$allConnections = @()
$deadline = (Get-Date).AddSeconds(4)
while ((Get-Date) -lt $deadline) {
    $children = Get-CimInstance Win32_Process -Filter "ParentProcessId = $mainId"
    foreach ($c in $children) { $allPids.Add([int]$c.ProcessId) | Out-Null }
    foreach ($cid in @($allPids)) {
        $gc = Get-CimInstance Win32_Process -Filter "ParentProcessId = $cid"
        foreach ($g in $gc) { $allPids.Add([int]$g.ProcessId) | Out-Null }
    }
    $conns = Get-NetTCPConnection -OwningProcess @($allPids) -ErrorAction SilentlyContinue
    if ($conns) { $allConnections += $conns }
    Start-Sleep -Milliseconds 50
}
```

### 4.7 後片付け

```powershell
Remove-Item -Recurse -Force $installDir
```

## 5. 署名なし配布に伴う事実

- インストーラ`COBOL Insight Setup 0.1.0.exe`にコード署名は施していない。
- `gui/package.json`の`build.win.signAndEditExecutable`は`false`である。この設定
  により、rcedit(実行ファイルへのバージョン情報書き込み)処理がスキップされ、
  `COBOL Insight.exe`・`COBOLInsight.exe`のいずれも製品名・説明・発行者等の
  バージョン情報が未設定のままである。
- SmartScreenの挙動(未署名インストーラ実行時の警告表示とその対処手順)は手動確認事項
  であり、`導入手順書.md`の手動確認欄で扱う。本書の測定範囲には含まない。
